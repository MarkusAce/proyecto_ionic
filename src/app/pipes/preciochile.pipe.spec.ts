import { PreciochilePipe } from './preciochile.pipe';

describe('PreciochilePipe', () => {
  it('create an instance', () => {
    const pipe = new PreciochilePipe();
    expect(pipe).toBeTruthy();
  });
});
