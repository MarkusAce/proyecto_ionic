import { FormatotarjetaPipe } from './formatotarjeta.pipe';

describe('FormatotarjetaPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatotarjetaPipe();
    expect(pipe).toBeTruthy();
  });
});
